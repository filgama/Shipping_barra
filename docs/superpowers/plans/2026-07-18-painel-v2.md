# Painel v2 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Timeline interativa ao toque, curva de maré, setas de direção, dark mode, secção "navios em porto", teste offline versionado e correção do bug do sector circular — conforme `docs/superpowers/specs/2026-07-18-painel-v2-design.md`.

**Architecture:** O Python continua a gerar todo o HTML (f-string em `gerar_html`); um bloco de JS vanilla (constante `JS_PAINEL`, fora do f-string) trata toque, marcador de "agora" e auto-scroll. Curva de maré é SVG gerado em Python. Testes em `teste_offline.py` (stdlib, `assert` puro), corridos no CI antes do deploy.

**Tech Stack:** Python 3.11+ stdlib apenas. Sem pytest, sem npm, sem dependências.

## Global Constraints

- Português europeu em código, comentários, UI e commits; terminologia náutica correta.
- Nenhum limiar de decisão náutica no código — só em `regras.toml` (o filtro de 30 dias de "em porto" é técnico e vive no código com comentário).
- HTML autocontido; design tokens: tinta `#1B2A38`, água `#DCEBF1`, papel `#F7F5EF`, magenta `#B0257C`; verde `#1E7A5A`, âmbar `#E2B93B`, vermelho `#C0392B`.
- Falhas de rede degradam com aviso, nunca crash silencioso.
- O banner de aviso ("Ferramenta informativa…") nunca é removido nem suavizado.
- ATENÇÃO: dentro do f-string de `gerar_html`, chavetas CSS/JS literais escrevem-se `{{ }}`. Por isso o JS vive na constante `JS_PAINEL` (string normal) inserida com `{JS_PAINEL}`.
- Comandos de verificação: `python -m py_compile janelas_barra.py` e `python teste_offline.py` (ambos a partir da raiz do projeto).

---

### Task 1: Harness de teste offline + bug do sector circular

**Files:**
- Create: `teste_offline.py`
- Modify: `janelas_barra.py` (função `avaliar_hora`, bloco `if "dir_min" in r:`)

**Interfaces:**
- Consumes: `avaliar_hora(hora, regras)`, `avaliar_ukc(calado, nivel_mar, regras)`, `extrair_navios(apl)` já existentes em `janelas_barra.py`.
- Produces: `teste_offline.py` com `REGRAS` (dict de regras fixas), `previsao_fixa() -> list[dict]` (12 horas sintéticas) e lista `TESTES` — as tasks seguintes acrescentam funções `teste_*` e registam-nas em `TESTES`.

- [ ] **Step 1: Criar `teste_offline.py` com fixtures e testes (o do sector circular vai falhar)**

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Teste offline — sem rede, sem dependências. Corre: python teste_offline.py
Obrigatório antes de commits que toquem no motor de regras, parser ou HTML."""

from datetime import datetime, timedelta

import janelas_barra as jb

REGRAS = {
    "canal": {"profundidade_zh": 15.0},
    "ukc": {"folga_minima_pct": 0.15, "folga_ambar_pct": 0.30},
    "regra": [
        {"parametro": "swell_altura", "descricao": "Swell", "ambar": 2.0,
         "vermelho": 3.0, "fonte": "PLACEHOLDER"},
        {"parametro": "vento_kn", "descricao": "Vento do quadrante N",
         "ambar": 16.0, "vermelho": 25.0, "dir_min": 300.0, "dir_max": 60.0,
         "fonte": "PLACEHOLDER"},
    ],
}


def previsao_fixa() -> list[dict]:
    """12 horas sintéticas: 0–5 verdes, 6–11 âmbar (swell 2.5 ≥ 2.0);
    nível do mar sinusoidal com PM em 02h/10h (+1.0) e BM em 06h (−1.0)."""
    niveis = [0.0, 0.6, 1.0, 0.6, 0.0, -0.6, -1.0, -0.6, 0.0, 0.6, 1.0, 0.6]
    return [{
        "tempo": f"2026-07-18T{i:02d}:00",
        "onda_altura": 1.0, "onda_dir": 300.0, "onda_periodo": 8.0,
        "swell_altura": 0.8 if i < 6 else 2.5,
        "swell_dir": 315.0, "swell_periodo": 12.0,
        "nivel_mar": niveis[i],
        "vento_kn": 10.0, "rajada_kn": 14.0, "vento_dir": 350.0,
    } for i in range(12)]


def teste_avaliar_hora_basico():
    prev = previsao_fixa()
    assert jb.avaliar_hora(prev[0], REGRAS)[0] == 0
    estado, motivos = jb.avaliar_hora(prev[6], REGRAS)
    assert estado == 1 and any("Swell" in m for m in motivos), motivos


def teste_setor_circular():
    # 350° cai no sector 300–060 que cruza o Norte
    hora = dict(previsao_fixa()[0], vento_kn=20.0, vento_dir=350.0)
    estado, motivos = jb.avaliar_hora(hora, REGRAS)
    assert estado == 1, f"vento 350° devia cair no sector 300–060 ({motivos})"
    hora_sul = dict(hora, vento_dir=180.0)
    assert jb.avaliar_hora(hora_sul, REGRAS)[0] == 0


def teste_ukc():
    # profundidade 15.0 + nível 0.5 = 15.5 m de água
    assert jb.avaliar_ukc(8.0, 0.5, REGRAS)[0] == 0    # folga 94 %
    assert jb.avaliar_ukc(12.0, 0.5, REGRAS)[0] == 1   # folga 29 % < 30 %
    assert jb.avaliar_ukc(13.5, 0.5, REGRAS)[0] == 2   # folga 14.8 % < 15 %
    assert jb.avaliar_ukc(None, 0.5, REGRAS) is None


def teste_extrair_navios():
    apl = {"chegadas": {"titulo": "t", "registos": [
        {"navio": "ALFA", "eta": "2026-07-18 10:00:00.0", "ata": "",
         "atd": "", "caladoMaxEntrada": 8.2, "nv_tipoNavio": "Carga",
         "zona": "CAIS X"},
        {"navio": "ALFA", "eta": "2026-07-18 10:00:00.0", "ata": "",
         "atd": "", "caladoMaxEntrada": 8.2},           # duplicado
        {"navio": "BRAVO", "eta": "2026-07-18 12:00:00.0",
         "ata": "2026-07-18 11:50:00.0"},               # já chegou
        {"navio": "CHARLIE", "eta": "data marada", "ata": "",
         "caladoMaxEntrada": "abc"},                    # tolerância a lixo
    ]}}
    navios = jb.extrair_navios(apl)
    assert [n["nome"] for n in navios] == ["ALFA", "CHARLIE"]
    assert navios[0]["calado"] == 8.2
    assert navios[1]["momento"] is None and navios[1]["calado"] is None


TESTES = [teste_avaliar_hora_basico, teste_setor_circular, teste_ukc,
          teste_extrair_navios]


def main():
    for t in TESTES:
        t()
        print(f"OK  {t.__name__}")
    print(f"\n{len(TESTES)} testes OK")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Correr e verificar que falha no sector circular**

Run: `python teste_offline.py`
Expected: `OK  teste_avaliar_hora_basico` seguido de `AssertionError: vento 350° devia cair no sector 300–060` em `teste_setor_circular`.

- [ ] **Step 3: Corrigir `avaliar_hora` (sector circular)**

Em `janelas_barra.py`, substituir o bloco direcional:

```python
        # setor direcional opcional
        if "dir_min" in r:
            dchave = dir_por_parametro.get(r["parametro"])
            d = hora.get(dchave) if dchave else None
            if d is None or not (r["dir_min"] <= d <= r["dir_max"]):
                continue
```

por:

```python
        # setor direcional opcional (suporta sectores que cruzam o Norte,
        # ex.: dir_min=300, dir_max=60)
        if "dir_min" in r:
            dchave = dir_por_parametro.get(r["parametro"])
            d = hora.get(dchave) if dchave else None
            if d is None:
                continue
            if r["dir_min"] <= r["dir_max"]:
                dentro = r["dir_min"] <= d <= r["dir_max"]
            else:
                dentro = d >= r["dir_min"] or d <= r["dir_max"]
            if not dentro:
                continue
```

- [ ] **Step 4: Correr e verificar que tudo passa**

Run: `python teste_offline.py`
Expected: `4 testes OK`

- [ ] **Step 5: Commit**

```bash
git add teste_offline.py janelas_barra.py
git commit -m "Teste offline versionado + correção do sector direcional circular"
```

---

### Task 2: Função `cardeal_seta` (graus → ponto cardeal + seta)

**Files:**
- Modify: `janelas_barra.py` (nova função a seguir a `avaliar_ukc`)
- Test: `teste_offline.py`

**Interfaces:**
- Produces: `cardeal_seta(graus) -> tuple[str, str]` — ex.: `cardeal_seta(315) == ("NW", "↘")`. Nome = proveniência (16 sectores); seta = rumo do fluxo (proveniência + 180°). `None` → `("", "")`. Usada na Task 3.

- [ ] **Step 1: Acrescentar o teste a `teste_offline.py` (e registá-lo em `TESTES`)**

```python
def teste_cardeal_seta():
    assert jb.cardeal_seta(315) == ("NW", "↘")   # swell de NW segue para SE
    assert jb.cardeal_seta(0) == ("N", "↓")
    assert jb.cardeal_seta(90) == ("E", "←")
    assert jb.cardeal_seta(None) == ("", "")
```

```python
TESTES = [teste_avaliar_hora_basico, teste_setor_circular, teste_ukc,
          teste_extrair_navios, teste_cardeal_seta]
```

- [ ] **Step 2: Correr e verificar que falha**

Run: `python teste_offline.py`
Expected: `AttributeError: module 'janelas_barra' has no attribute 'cardeal_seta'`

- [ ] **Step 3: Implementar em `janelas_barra.py`, logo após `avaliar_ukc`**

```python
SETORES = ["N", "NNE", "NE", "ENE", "E", "ESE", "SE", "SSE",
           "S", "SSW", "SW", "WSW", "W", "WNW", "NW", "NNW"]


def cardeal_seta(graus) -> tuple[str, str]:
    """Direção meteorológica (proveniência) → (ponto cardeal, seta do rumo).
    Convenção náutica: 'swell de NW' — o nome diz de onde vem; a seta
    aponta para onde o fluxo segue (proveniência + 180°)."""
    if graus is None:
        return "", ""
    g = float(graus) % 360
    nome = SETORES[int((g + 11.25) // 22.5) % 16]
    rumo = (g + 180) % 360
    seta = "↑↗→↘↓↙←↖"[int(((rumo + 22.5) % 360) // 45)]
    return nome, seta
```

- [ ] **Step 4: Correr e verificar que passa**

Run: `python teste_offline.py`
Expected: `5 testes OK`

- [ ] **Step 5: Commit**

```bash
git add teste_offline.py janelas_barra.py
git commit -m "cardeal_seta: graus para ponto cardeal e seta de rumo"
```

---

### Task 3: Timeline interativa (data-*, painel de detalhe, "agora", contraste, auto-refresh)

**Files:**
- Modify: `janelas_barra.py` (constante `JS_PAINEL` junto a `COR`; `gerar_html`: loop das células, CSS, corpo da secção timeline, `<head>`)
- Test: `teste_offline.py`

**Interfaces:**
- Consumes: `cardeal_seta` (Task 2).
- Produces: células com `data-t`/`data-estado`/`data-motivos`/`data-vals`; `<div id="detalhe" hidden>`; classes CSS `.scroll`, `.cel-a`, `.cel.agora`, `.cel.sel`; constante `JS_PAINEL` inserida antes de `</body>`. A Task 4 insere o SVG dentro de `<div class="scroll">`.

- [ ] **Step 1: Acrescentar o teste (e registá-lo em `TESTES`)**

```python
def teste_html_timeline_interativa():
    prev = previsao_fixa()
    avals = [jb.avaliar_hora(h, REGRAS) for h in prev]
    out = jb.gerar_html(prev, avals, [], {}, REGRAS)
    assert "Ferramenta informativa" in out            # aviso obrigatório
    assert 'id="detalhe"' in out
    assert "data-t='2026-07-18T00:00'" in out
    assert "data-estado='ambar'" in out               # horas 6–11
    assert "cel-a" in out                             # contraste no âmbar
    assert "NW↘" in out                               # setas nos data-vals
    assert 'http-equiv="refresh"' in out
    assert "scrollIntoView" in out                    # JS presente
```

- [ ] **Step 2: Correr e verificar que falha**

Run: `python teste_offline.py`
Expected: `AssertionError` no primeiro `assert` novo (`id="detalhe"`).

- [ ] **Step 3: Implementar**

3a. Constante `JS_PAINEL` ao nível do módulo, logo após `COR = {...}` (string normal — chavetas sem escape):

```python
JS_PAINEL = """
<script>
(function () {
  var det = document.getElementById('detalhe');
  var cels = Array.prototype.slice.call(document.querySelectorAll('.cel'));
  function mostrar(c) {
    cels.forEach(function (x) { x.classList.remove('sel'); });
    c.classList.add('sel');
    var t = c.dataset.t;
    var linhas = ['<b>' + t.slice(8, 10) + '/' + t.slice(5, 7) + ' ' +
                  t.slice(11, 13) + 'h</b> — ' + c.dataset.estado];
    if (c.dataset.motivos) linhas.push(c.dataset.motivos);
    if (c.dataset.vals) linhas.push(c.dataset.vals);
    det.innerHTML = linhas.join('<br>');
    det.hidden = false;
  }
  cels.forEach(function (c) {
    c.addEventListener('click', function () { mostrar(c); });
  });
  // marcador de "agora" pela hora do dispositivo (correto mesmo com cache)
  var ag = new Date();
  function p2(n) { return String(n).padStart(2, '0'); }
  var iso = ag.getFullYear() + '-' + p2(ag.getMonth() + 1) + '-' +
            p2(ag.getDate()) + 'T' + p2(ag.getHours()) + ':00';
  var atual = document.querySelector('.cel[data-t="' + iso + '"]');
  if (atual) {
    atual.classList.add('agora');
    atual.scrollIntoView({ inline: 'center', block: 'nearest' });
    mostrar(atual);
  }
})();
</script>"""
```

3b. Em `gerar_html`, substituir o loop das células por:

```python
    celulas = []
    for hora, (estado, motivos) in zip(previsao, avaliacoes):
        t = hora["tempo"]  # "2026-07-18T14:00"
        dia, hh = t[8:10], t[11:13]
        vals = []
        if hora.get("swell_altura") is not None:
            nome, seta = cardeal_seta(hora.get("swell_dir"))
            per = (f" {hora['swell_periodo']:g} s"
                   if hora.get("swell_periodo") is not None else "")
            vals.append(f"swell {hora['swell_altura']:g} m {nome}{seta}{per}")
        if hora.get("nivel_mar") is not None:
            vals.append(f"nível {hora['nivel_mar']:+g} m")
        if hora.get("vento_kn") is not None:
            nome, seta = cardeal_seta(hora.get("vento_dir"))
            raj = (f" (raj {hora['rajada_kn']:g})"
                   if hora.get("rajada_kn") is not None else "")
            vals.append(f"vento {hora['vento_kn']:g} kn {nome}{seta}{raj}")
        vals_txt = " · ".join(vals)
        mot_txt = "; ".join(motivos)
        tip = f"{dia}/{t[5:7]} {hh}h — {ESTADO_NOME[estado]}"
        if mot_txt:
            tip += " · " + mot_txt
        if vals_txt:
            tip += " · " + vals_txt
        marca_dia = f"<span class='dia'>{dia}</span>" if hh == "00" else ""
        extra_cls = " cel-a" if estado == 1 else ""
        celulas.append(
            f"<div class='cel{extra_cls}' style='background:{COR[estado]}' "
            f"title=\"{e(tip)}\" data-t='{t}' "
            f"data-estado='{ESTADO_NOME[estado]}' "
            f"data-motivos=\"{e(mot_txt)}\" data-vals=\"{e(vals_txt)}\">"
            f"{marca_dia}<span class='hh'>{hh}</span></div>")
```

3c. No CSS do f-string (chavetas duplicadas!), substituir a linha
`.timeline {{ display:flex; overflow-x:auto; gap:2px; padding-bottom:6px; }}` por:

```css
 .scroll {{ overflow-x:auto; padding-bottom:6px; }}
 .timeline {{ display:flex; gap:2px; }}
 .cel.agora {{ outline:3px solid var(--mag); outline-offset:-1px; }}
 .cel.sel {{ box-shadow:inset 0 0 0 2px var(--tinta); }}
 .cel-a .hh, .cel-a .dia {{ color:#1B2A38; }}
 #detalhe {{ margin-top:8px; padding:8px 10px; border:1.5px dashed var(--tinta);
            border-radius:8px; font-size:13px; line-height:1.45; }}
```

3d. No corpo, substituir
`<div class="timeline">{''.join(celulas)}</div>` por:

```html
 <div class="scroll">
  <div class="timeline">{''.join(celulas)}</div>
 </div>
 <div id="detalhe" hidden></div>
```

3e. No `<head>`, após a meta viewport: `<meta http-equiv="refresh" content="900">`.

3f. Antes de `</body>`: `{JS_PAINEL}`.

- [ ] **Step 4: Correr e verificar que passa**

Run: `python -m py_compile janelas_barra.py && python teste_offline.py`
Expected: `6 testes OK`

- [ ] **Step 5: Commit**

```bash
git add teste_offline.py janelas_barra.py
git commit -m "Timeline interativa: detalhe ao toque, marcador de agora, contraste e auto-refresh"
```

---

### Task 4: Curva de maré (SVG server-rendered)

**Files:**
- Modify: `janelas_barra.py` (nova função `gerar_svg_mare` antes de `gerar_html`; integração no corpo e CSS)
- Test: `teste_offline.py`

**Interfaces:**
- Consumes: estrutura `previsao` (lista de dicts com `nivel_mar`, `tempo`); `<div class="scroll">` da Task 3.
- Produces: `gerar_svg_mare(previsao, passo=24, alto=56) -> str` — SVG alinhado às células (`passo` px/hora, célula 22px + gap 2px) ou `""` sem dados.

- [ ] **Step 1: Acrescentar o teste (e registá-lo em `TESTES`)**

```python
def teste_svg_mare():
    prev = previsao_fixa()
    svg = jb.gerar_svg_mare(prev)
    assert svg.startswith("<svg") and "polyline" in svg
    assert "02h +1.0" in svg and "06h -1.0" in svg     # PM e BM anotadas
    sem_dados = [dict(h, nivel_mar=None) for h in prev]
    assert jb.gerar_svg_mare(sem_dados) == ""
    avals = [jb.avaliar_hora(h, REGRAS) for h in prev]
    assert "<svg" in jb.gerar_html(prev, avals, [], {}, REGRAS)
```

- [ ] **Step 2: Correr e verificar que falha**

Run: `python teste_offline.py`
Expected: `AttributeError: ... no attribute 'gerar_svg_mare'`

- [ ] **Step 3: Implementar**

3a. Antes de `gerar_html`:

```python
def gerar_svg_mare(previsao, passo=24, alto=56) -> str:
    """Curva do nível do mar (modelo, rel. MSL) alinhada à timeline:
    um `passo` por hora. Preia-mares/baixa-mares anotadas com hora e
    altura. Devolve "" se não houver dados suficientes."""
    valores = [h.get("nivel_mar") for h in previsao]
    pares = [(i, v) for i, v in enumerate(valores) if v is not None]
    if len(pares) < 3:
        return ""
    vmin = min(v for _, v in pares)
    vmax = max(v for _, v in pares)
    amp = (vmax - vmin) or 1.0
    m_topo, m_base = 14, 8
    util = alto - m_topo - m_base

    def xy(i, v):
        return i * passo + passo / 2, m_topo + (vmax - v) / amp * util

    pontos = " ".join(f"{x:.0f},{y:.1f}"
                      for x, y in (xy(i, v) for i, v in pares))
    marcas = []
    for k in range(1, len(pares) - 1):
        i, v = pares[k]
        antes, depois = pares[k - 1][1], pares[k + 1][1]
        pm = v >= antes and v > depois
        bm = v <= antes and v < depois
        if not (pm or bm):
            continue
        x, y = xy(i, v)
        dy = -4 if pm else 12
        marcas.append(f"<text x='{x:.0f}' y='{y + dy:.1f}' "
                      f"text-anchor='middle' class='mare-rot'>"
                      f"{previsao[i]['tempo'][11:13]}h {v:+.1f}</text>")
    largura = len(previsao) * passo
    linha0 = ""
    if vmin <= 0 <= vmax:
        y0 = m_topo + vmax / amp * util
        linha0 = (f"<line x1='0' y1='{y0:.1f}' x2='{largura}' "
                  f"y2='{y0:.1f}' class='mare-msl'/>")
    return (f"<svg class='mare' width='{largura}' height='{alto}' "
            f"viewBox='0 0 {largura} {alto}'>{linha0}"
            f"<polyline points='{pontos}' class='mare-linha'/>"
            f"{''.join(marcas)}</svg>")
```

3b. Em `gerar_html`, antes do `return`, calcular `svg_mare = gerar_svg_mare(previsao)` e, no corpo, inserir `{svg_mare}` dentro de `<div class="scroll">`, imediatamente a seguir ao `</div>` da `.timeline`. Na legenda, acrescentar ` · curva: nível modelado (rel. MSL)` quando `svg_mare` não for vazio (usar variável `legenda_mare = " · curva: nível modelado (rel. MSL)" if svg_mare else ""`).

3c. CSS (chavetas duplicadas):

```css
 .mare {{ display:block; }}
 .mare-linha {{ fill:none; stroke:#3B7EA1; stroke-width:2; }}
 .mare-rot {{ font-size:9px; fill:currentColor; }}
 .mare-msl {{ stroke:#5C6E7C; stroke-dasharray:3 3; }}
```

- [ ] **Step 4: Correr e verificar que passa**

Run: `python -m py_compile janelas_barra.py && python teste_offline.py`
Expected: `7 testes OK`

- [ ] **Step 5: Commit**

```bash
git add teste_offline.py janelas_barra.py
git commit -m "Curva de maré SVG alinhada à timeline, com PM/BM anotadas"
```

---

### Task 5: Secção "Em porto agora" (novo serviço APL + filtro)

**Files:**
- Modify: `janelas_barra.py` (`SERVICOS_APL`, `recolher_apl`, nova função `filtrar_em_porto` após `extrair_navios`, secção no `gerar_html`)
- Test: `teste_offline.py`

**Interfaces:**
- Consumes: `_momento(texto)` já existente; `apl` dict passado a `gerar_html`.
- Produces: `filtrar_em_porto(registos, agora=None) -> list[dict]` com chaves `nome`, `ata` (datetime), `etd` (datetime|None), `tipo`, `zona`, ordenada por ATA descendente; `recolher_apl` passa a devolver também a chave `"em_porto"`.

- [ ] **Step 1: Acrescentar os testes (e registá-los em `TESTES`)**

```python
def teste_filtrar_em_porto():
    agora = datetime(2026, 7, 18, 12, 0)
    registos = [
        {"navio": "DELTA", "ata": "2026-07-17 08:00:00.0", "atd": "",
         "etd": "2026-07-19 10:00:00.0", "nv_tipoNavio": "Carga",
         "zona": "CAIS Y"},
        {"navio": "ECO", "ata": "2020-11-10 14:22:00.0", "atd": ""},   # fóssil
        {"navio": "FOX", "ata": "2026-07-17 09:00:00.0",
         "atd": "2026-07-18 06:00:00.0"},                              # já saiu
        {"navio": "DELTA", "ata": "2026-07-17 08:00:00.0", "atd": ""}, # dup
        {"navio": "GOLF", "ata": "", "atd": ""},                       # sem ATA
    ]
    porto = jb.filtrar_em_porto(registos, agora=agora)
    assert [n["nome"] for n in porto] == ["DELTA"]
    assert porto[0]["etd"] == datetime(2026, 7, 19, 10, 0)


def teste_html_em_porto():
    ata = (datetime.now() - timedelta(days=1)).strftime("%Y-%m-%d %H:%M:%S.0")
    apl = {"em_porto": {"titulo": "t", "registos": [
        {"navio": "DELTA", "ata": ata, "atd": "",
         "etd": "2099-01-01 10:00:00.0", "nv_tipoNavio": "Carga",
         "zona": "CAIS Y"}]}}
    prev = previsao_fixa()
    avals = [jb.avaliar_hora(h, REGRAS) for h in prev]
    out = jb.gerar_html(prev, avals, [], apl, REGRAS)
    assert "Em porto agora (1)" in out and "DELTA" in out
```

- [ ] **Step 2: Correr e verificar que falha**

Run: `python teste_offline.py`
Expected: `AttributeError: ... no attribute 'filtrar_em_porto'`

- [ ] **Step 3: Implementar**

3a. `SERVICOS_APL` passa a ter um terceiro elemento (serviço com/sem datas):

```python
SERVICOS_APL = {
    "chegadas": ("Previsão de Chegadas (ETA)",
                 "/apl.processosweb/get-chegadas", True),
    "partidas": ("Previsão de Partidas (ETD)",
                 "/apl.processosweb/get-partidas", True),
    "em_porto": ("Navios em Porto",
                 "/apl.processoswebemporto/get-navios-em-porto", False),
}
```

3b. Em `recolher_apl`, o loop passa a desempacotar três valores e a escolher o corpo:

```python
    for chave, (titulo, servico, com_datas) in SERVICOS_APL.items():
        print(f"[APL] {titulo} … ", end="", flush=True)
        try:
            corpo = json.dumps({servico: params if com_datas else {}}).encode()
```

(o resto da função mantém-se igual)

3c. Após `extrair_navios`:

```python
# Filtro técnico (não é limiar de decisão náutica): o serviço "em porto"
# devolve escalas históricas antigas; só mostramos ATAs recentes.
JANELA_PORTO_DIAS = 30


def filtrar_em_porto(registos, agora=None) -> list[dict]:
    """Navios atracados: ATA preenchida, ATD vazia, ATA recente.
    Deduplica por nome; ordena da chegada mais recente para a mais antiga."""
    agora = agora or datetime.now()
    out, vistos = [], set()
    for reg in registos:
        ata = _momento(reg.get("ata"))
        if ata is None or str(reg.get("atd") or "").strip():
            continue
        if (agora - ata).days > JANELA_PORTO_DIAS:
            continue
        nome = (reg.get("navio") or reg.get("nv_nome") or "?").strip()
        if nome in vistos:
            continue
        vistos.add(nome)
        out.append({"nome": nome, "ata": ata,
                    "etd": _momento(reg.get("etd")),
                    "tipo": (reg.get("nv_tipoNavio") or "").strip(),
                    "zona": (reg.get("zona") or "").strip()})
    out.sort(key=lambda n: n["ata"], reverse=True)
    return out
```

3d. Em `gerar_html`, antes do `return`, montar a secção:

```python
    em_porto = filtrar_em_porto(
        apl.get("em_porto", {}).get("registos", []))
    linhas_porto = []
    for n in em_porto:
        etd = n["etd"].strftime("%d/%m %H:%M") if n["etd"] else "—"
        meta = " · ".join(x for x in (n["tipo"], n["zona"]) if x)
        linhas_porto.append(
            f"<div class='navio'><span class='farol' "
            f"style='background:#5C6E7C'></span><div>"
            f"<div class='nnome'>{e(n['nome'])}</div>"
            f"<div class='nmeta'>{e(meta)}</div>"
            f"<div class='nmeta'>ETD prevista: {etd}</div></div></div>")
    seccao_porto = ("".join(linhas_porto) or
                    "<p class='vazio'>Sem navios em porto nesta recolha.</p>")
```

e, no corpo, entre a secção dos navios ETA/ETD e a das regras:

```html
<section>
 <h2>Em porto agora ({len(em_porto)})</h2>
 {seccao_porto}
</section>
```

- [ ] **Step 4: Correr e verificar que passa**

Run: `python -m py_compile janelas_barra.py && python teste_offline.py`
Expected: `9 testes OK`

- [ ] **Step 5: Commit**

```bash
git add teste_offline.py janelas_barra.py
git commit -m "Secção Em porto agora via serviço navios-em-porto da APL"
```

---

### Task 6: Dark mode + theme-color

**Files:**
- Modify: `janelas_barra.py` (`gerar_html`: CSS e `<head>`)
- Test: `teste_offline.py`

**Interfaces:**
- Consumes: tokens CSS existentes (`--tinta`, `--agua`, `--papel`, `--mag`).
- Produces: bloco `@media (prefers-color-scheme: dark)` e duas metas `theme-color`.

- [ ] **Step 1: Acrescentar o teste (e registá-lo em `TESTES`)**

```python
def teste_dark_mode():
    prev = previsao_fixa()
    avals = [jb.avaliar_hora(h, REGRAS) for h in prev]
    out = jb.gerar_html(prev, avals, [], {}, REGRAS)
    assert "prefers-color-scheme: dark" in out
    assert out.count('name="theme-color"') == 2
```

- [ ] **Step 2: Correr e verificar que falha**

Run: `python teste_offline.py`
Expected: `AssertionError` em `prefers-color-scheme`.

- [ ] **Step 3: Implementar**

3a. No `<head>`, após a meta refresh:

```html
<meta name="theme-color" content="#DCEBF1" media="(prefers-color-scheme: light)">
<meta name="theme-color" content="#0D1720" media="(prefers-color-scheme: dark)">
```

3b. No fim do `<style>` (chavetas duplicadas; os tokens invertem-se — tinta
clara sobre fundos escuros — e o resto herda):

```css
 @media (prefers-color-scheme: dark) {{
  :root {{ --tinta:#E6EDF3; --agua:#0D1720; --papel:#15222D; --mag:#E464AE; }}
  section {{ border-color:#33475A; }}
  th {{ background:#0D1720; }}
  th, td {{ border-color:#33475A; }}
  .navio {{ border-bottom-color:#243442; }}
  .aviso {{ background:#15222D; color:#E6EDF3;
           border-bottom:1px solid #33475A; }}
  .fonte-ok {{ color:#4CC38A; }}
 }}
```

Nota: `.cel-a .hh` mantém `#1B2A38` fixo — o fundo âmbar não muda com o tema.

- [ ] **Step 4: Correr e verificar que passa**

Run: `python -m py_compile janelas_barra.py && python teste_offline.py`
Expected: `10 testes OK`

- [ ] **Step 5: Commit**

```bash
git add teste_offline.py janelas_barra.py
git commit -m "Dark mode automático e theme-color"
```

---

### Task 7: CI corre o teste offline + documentação

**Files:**
- Modify: `.github/workflows/atualizar.yml`
- Modify: `CLAUDE.md`
- Modify: `README.md`

**Interfaces:**
- Consumes: `teste_offline.py` completo (Tasks 1–6).
- Produces: gate de qualidade no CI; documentação alinhada com o código.

- [ ] **Step 1: Acrescentar o passo de teste ao workflow, antes de "Gerar painel"**

```yaml
      - name: Teste offline
        run: python teste_offline.py
```

- [ ] **Step 2: Atualizar `CLAUDE.md`**

2a. Na secção Estrutura, acrescentar a linha do ficheiro de teste após `janelas_barra.py`:

```
├── teste_offline.py          # teste sem rede: fixtures + assert (correr antes de commit)
```

2b. Na secção Convenções, substituir "Um só ficheiro Python enquanto for razoável (< ~1000 linhas)" — se ainda disser "~700", atualizar para "~1000".

2c. Na secção Comandos, substituir o bloco de "Teste offline (sem rede): construir uma lista `previsao` fictícia…" por:

```markdown
Teste offline (sem rede): `python teste_offline.py` — fixtures sintéticas
para `avaliar_hora` (incl. sectores que cruzam o Norte), `avaliar_ukc`,
`extrair_navios`, `filtrar_em_porto`, `gerar_svg_mare` e `gerar_html`.
Obrigatório antes de qualquer commit que toque no motor de regras, no
parser ou no HTML; o CI corre-o antes de publicar.
```

e acrescentar ao bloco de comandos:

```bash
python teste_offline.py                # teste offline (obrigatório antes de commit)
```

- [ ] **Step 3: Atualizar `README.md`** — na secção "Publicar para o telemóvel", acrescentar no fim:

```markdown
No painel, toca numa hora da timeline para veres os motivos e valores
(swell, nível, vento); a hora atual aparece contornada a magenta.
```

- [ ] **Step 4: Verificação completa**

Run: `python -m py_compile janelas_barra.py && python teste_offline.py`
Expected: `10 testes OK`

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/atualizar.yml CLAUDE.md README.md
git commit -m "CI corre o teste offline antes de publicar; docs atualizadas"
```

---

### Task 8: Verificação end-to-end real e publicação

**Files:**
- Modify: `index.html` (regenerado)

**Interfaces:**
- Consumes: tudo o que foi construído nas Tasks 1–7.
- Produces: painel v2 em produção.

- [ ] **Step 1: Recolha real completa**

Run: `python janelas_barra.py --horas 72`
Expected: linhas `[APL] … registos` para chegadas, partidas E navios em porto; `[OK] Painel: …index.html`.

- [ ] **Step 2: Inspecionar o HTML gerado**

Run (Grep sobre `index.html`): confirmar presença de `data-t='`, `id="detalhe"`, `<svg class='mare'`, `Em porto agora (`, `prefers-color-scheme`, `scrollIntoView`, e do banner "Ferramenta informativa".
Expected: todas presentes; contagem de `class='cel` igual a 72.

- [ ] **Step 3: Abrir o painel localmente no browser para verificação visual**

Run: `start index.html` (Windows). Verificar: toque/clique numa célula abre o detalhe; célula "agora" contornada; curva de maré alinhada; secção em porto preenchida.

- [ ] **Step 4: Commit do painel regenerado e push**

```bash
git add index.html
git commit -m "Painel v2 gerado com timeline interativa, maré e em porto"
git push
```

- [ ] **Step 5: Publicar e verificar produção**

Disparar o workflow (botão "Run workflow" no GitHub, pelo utilizador) ou aguardar o agendamento de 30 min; depois:

Run: `curl -s "https://filgama.github.io/Shipping_barra/"` e verificar as mesmas strings do Step 2.
Expected: painel v2 em produção.
